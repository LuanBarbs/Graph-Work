#ifndef DEFINES_HPP_INCLUDED
#define DEFINES_HPP_INCLUDED

// DEFINIÇÕES ÚTEIS
#include <algorithm> /* Implementação de algoritmos de ordenação, soma acumulada, reversão de vetores e máximo e mínimos de containers. */
#include <cmath>     /* Implementação de algoritmos matemáticos e estatísticos. */
#include <fstream>   /* Manipulação de arquivos. */
#include <iomanip>   /* Manipulação de saída e entrada de dados. */
#include <iostream>  /* Implementações básicas da linguagem. */
#include <utility>   /* Implementações de alguns containers e operações de swap. */
#include <vector>    /* Implementação do container vector e suas operações. */

#include <random>

#endif // DEFINES_HPP_INCLUDED
